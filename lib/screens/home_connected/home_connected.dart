import 'package:flutter/material.dart';

class HomeConnected extends StatelessWidget {
  const HomeConnected({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Text("connected bruh"),
    );
  }
}
